<!-- doc_type: explanation -->

# Stripper fixture

This page carries every construct the stripper removes.

| Flag | Meaning |
|---|---|
| `--root` | tree to walk |

Prose with a `code span`, an [inline link](https://example.test/page) and an
![image](./logo.png) that all reduce to their visible words.

```bash
echo "this fenced text is not prose"
```

!!! note "A callout marker"
    The callout body is prose and stays.

<<< @/snippets/example.py

<div class="grid">A raw HTML wrapper.</div>

[ref]: https://example.test/reference
