# Generated by ocx-mirror v{OCX_MIRROR_VERSION} (rev {OCX_MIRROR_REV})
# DO NOT EDIT — re-run `ocx-mirror package pipeline generate ci` to update.
# Source: {SPEC_SOURCE}

name: describe{WORKFLOW_SUFFIX}

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
{TRIGGER_PATHS}

concurrency:
  group: mirror-${{ github.workflow }}-describe
  cancel-in-progress: false

jobs:
  describe:
    runs-on: ubuntu-latest
{DESCRIBE_PERMISSIONS}    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - uses: ocx-sh/setup-ocx@de8e3366f812941423985eaccae28663ef192e8b  # v1.3.0
{REGISTRY_AUTH_STEPS}      - name: Publish catalog metadata
        if: ${{ steps.creds.outputs.have == 'true' }}
        run: |
          ocx-mirror package pipeline describe{SPEC_ARG}
