# Generated by ocx-mirror v{OCX_MIRROR_VERSION} (rev {OCX_MIRROR_REV})
# DO NOT EDIT — re-run `ocx-mirror pipeline generate ci` to update.
# Source: mirror.yml

name: verify-generated

# Fail closed when a generated workflow is hand-edited: this job re-renders from
# mirror.yml and diffs against what is committed. Re-run
# `ocx-mirror pipeline generate ci` locally and commit the result to fix drift.
# To opt out (discouraged), set `allow_manual_edits: true` in mirror.yml — the
# repository then owns its workflows by hand and this guard is not emitted.
on:
  pull_request:
    paths:
      - mirror.yml
      - scripts/**
      - tests/**
      - metadata*.json
      - .github/workflows/**
  push:
    branches: [main]
    paths:
      - mirror.yml
      - scripts/**
      - tests/**
      - metadata*.json
      - .github/workflows/**

permissions:
  contents: read

jobs:
  verify-generated:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - uses: ocx-sh/setup-ocx@v1
      - name: Check generated workflows are up to date
        # Exits 65 (DataError) on drift; the path-only hint names the stale file.
        run: |
          ocx-mirror pipeline generate ci --check
