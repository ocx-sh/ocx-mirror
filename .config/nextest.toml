[profile.ci]
default-filter = "all()"
fail-fast = false

[profile.ci.junit]
path = "junit.xml"

# Local unit telemetry (plan D-B5): `task rust:test:nextest` pushes this file.
[profile.default.junit]
path = "junit.xml"
