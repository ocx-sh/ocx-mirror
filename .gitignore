# =============================================================================
# ocx-mirror
# =============================================================================
target/
.task/
out/
.user.env
test/.venv/
test/.pytest_cache/
test/.out/
test/bin/
.mirror-work/
site/

# =============================================================================
# Claude Code Local Files
# =============================================================================
CLAUDE.local.md
.claude/settings.local.json
.claude/state/
.claude/reports/

# =============================================================================
# OS / Editors
# =============================================================================
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# =============================================================================
# Python
# =============================================================================
__pycache__/
*.pyc
.venv/
venv/

# =============================================================================
# Logs / runtime
# =============================================================================
*.log
*.pid
*.sock

# Agent worktrees (see workflow-swarm.md) — never committed.
# Only the transient checkouts; .agents/memory/ is team-shared and tracked.
.agents/worktrees/

# Goal-run drafts and long-job logs (survive the hourly target/ and /tmp wipes).
/.tmp/

# Bazel (adr_bazel_crate_split.md § C7). The crate_universe lockfile records
# the generating checkout's absolute paths — `task bazel:bootstrap` regenerates it.
/Cargo.bazel.lock.json
/bazel-*
/.bazelrc.user
