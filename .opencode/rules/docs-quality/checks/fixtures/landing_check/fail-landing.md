<!-- doc_type: landing -->
<!-- doc_tier: first-steps -->

# Example Platform

Example Platform is the layer that sits between your team and the registry it
depends on. It replaces the scripts every team writes twice. It is trusted by
2,000 developers. Coming soon: a hosted control plane.

We built it after watching three teams solve the same distribution problem in
three incompatible ways, and we decided the fix belonged one layer down. This
paragraph keeps going so that no runnable command and no link menu appears
anywhere before the word budget runs out. There is nothing here a reader can
click, and nothing here a reader can run, which is the exact defect the rule
exists to catch on a page whose whole job is to hand the reader a next step
within the first screen of prose.

That is the end of the positioning prose, and still no action has appeared.
