[package]
name = "ocx_mirror_test_support"
description = "Test-only scaffolding shared by the ocx-mirror workspace: the process-environment lock and its guards, a fake `ocx` push, the rustls provider install"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
publish.workspace = true

# Dev-only: every member takes this as a `[dev-dependencies]` row, never a
# normal one (`crates/crate_map.toml` [dev], enforced by
# `tests/workspace_structure.rs`).
[dependencies]
rustls.workspace = true
tokio.workspace = true

[lints]
workspace = true
