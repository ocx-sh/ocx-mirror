# Generated by ocx-mirror v{OCX_MIRROR_VERSION} (rev {OCX_MIRROR_REV})
# DO NOT EDIT — re-run `ocx-mirror package pipeline generate ci --spec {SPEC_FILE}` to update.
# Source: {SPEC_FILE}

name: verify-{SLUG}

# Fail closed when a generated workflow is hand-edited: this job re-renders from
# {SPEC_FILE} and diffs against what is committed. Re-run
# `ocx-mirror package pipeline generate ci --spec {SPEC_FILE}` locally and commit the
# result to fix drift. To opt out (discouraged), set `allow_manual_edits: true` in
# {SPEC_FILE} — the repository then owns its workflows by hand and this guard is not emitted.
on:
  pull_request:
    paths:
      - {SPEC_FILE}
      - scripts/**
      - tests/**
      - metadata*.json
      - .github/workflows/**
  push:
    branches: [main]
    paths:
      - {SPEC_FILE}
      - scripts/**
      - tests/**
      - metadata*.json
      - .github/workflows/**

permissions:
  contents: read

jobs:
  verify-generated:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
      - uses: ocx-sh/setup-ocx@8cd2f38eed07c9b79aaed22e61f9ba6b36e967a3  # v1.2.2
      - name: Check generated workflows are up to date
        # Exits 65 (DataError) on drift; the path-only hint names the stale file.
        run: |
          ocx-mirror package pipeline generate ci --check --spec {SPEC_FILE}
