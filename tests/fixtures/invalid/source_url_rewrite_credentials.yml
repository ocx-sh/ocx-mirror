# expect: must not embed credentials
name: test
target:
  registry: localhost:5000
  repository: test
source:
  type: url_index
  url: "https://example.com/versions.json"
  url_rewrite:
    from: "https://example.com/"
    to: "https://user:pass@artifactory.example.com/artifactory/remote/"
assets:
  linux/amd64:
    - "test\\.tar\\.gz"
