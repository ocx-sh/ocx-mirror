# expect: versions.max.version.generator.command must be a non-empty list
name: test
target:
  registry: localhost:5000
  repository: test
source:
  type: github_release
  owner: test
  repo: test
assets:
  linux/amd64:
    - "test\\.tar\\.gz"
versions:
  max:
    version:
      generator:
        command: []
    inclusive: false
