# expect: versions.max.version.url must not embed credentials
name: test
target:
  registry: localhost:5000
  repository: test
source:
  type: github_release
  owner: test
  repo: test
assets:
  linux/amd64:
    - "test\\.tar\\.gz"
versions:
  max:
    version:
      url: https://user:pass@example.com/stable
    inclusive: false
